The source controls now belong to the dashboard header. Company, game time, sites, staff and the In browser dot share one masthead. Directly beneath it, an unboxed status row places the result first, filename and save timestamp second, with Update and More together on the right. Navigation completes the header. The In browser dot describes where the board runs; only the source row describes freshness.

Landing uses the same width, masthead rule and typography. One sentence explains the tool, one primary action chooses the folder, and drop-anywhere stays visible without a separate drop-zone card. One save file remains available. The game-text chip has its own short explanation, since missing text also removes recipes and capacities. Save help is expanded in the reference to expose both copyable paths, including the locale directory, and the macOS alternative.

Return visits lead with Open newest save. Folder remembered describes stored access, not an already-loaded company. The permission hint sits beside that state. Changing the folder and choosing one file remain secondary. This state applies to browsers with a remembered directory handle; snapshot-based browsers retain the picker flow.

Busy and error states use the same source-row geometry. Reading disables Update and explicitly identifies the last good snapshot still on screen. Errors retain that board, show the explanation below the status, and expose the existing folder picker as recovery. No newer save is a successful check, not a warning. Checkboxes above Landing and Board: up to date expose runtime loading and no-newer-save variants. These are reference controls, not proposed product features. Runtime loading uses an indeterminate indicator, with reduced-motion support.

Today keeps four metrics in their existing order. Shared rules replace separate card outlines; exact figures remain prominent. Profit comparisons and cash reinvestment are split into scan-friendly lines. Findings align kind, action, amount and deadline. Filter kinds, details links and smaller findings remain represented. Results, Supply, Growth and Company content, calculations, thresholds and routing are unchanged.

Both existing palettes are preserved verbatim and follow prefers-color-scheme. The layout keeps four KPIs at 900px and wraps further below that. Archivo and IBM Plex Mono are the only external resources.

This is a static reference: native disclosures, file inputs and Copy work; Update, opening remembered saves, history clearing and dashboard routing are intentionally unwired. No save is parsed or uploaded. Company totals use the request’s Costco sample; README supplies the hype, sugar and cash-flow examples. Supporting KPI figures, liquor finding and sparkline samples come from dashboard.html. They illustrate the layout, not one reconciled save.
